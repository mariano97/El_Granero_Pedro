<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="surabastos.pedro.granero.com.el_granero_pedro">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />


    <application
        android:name=".util.Utilss"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        android:supportsRtl="true">
        <activity android:name=".controller.MainActivity"
            android:theme="@style/AppThemePrincipal">

        </activity>
        <activity
            android:name=".controller.SplashActivity"
            android:theme="@style/splashScreem">

        </activity>
        <activity
            android:name=".controller.LogIn"
            android:theme="@style/AppThemePrincipal">

        </activity>
        <activity android:name=".controller.PrincipalActivity" android:label="Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

<!-- android:label="@string/title_activity_log_in" -->